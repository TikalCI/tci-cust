#seed_jobs:  #uncomment this line to overwrite the default setup. Lease as is for adding setup
  tci-maven-app-ci-build:
    source:
      remote: https://github.com/TikalCI/tci-app-set.git
      branch: 'master'
    pipeline: apps/maven-app/Jenkinsfile
    executeWhen: never

